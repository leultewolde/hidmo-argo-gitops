resources:
  - deployment.yaml
  - service.yaml
  - db-job.yaml
  - migrate-job.yaml
  - secret.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: micro1-migrations
    files:
      - sql/V1__init.sql
  - name: micro1-create-db
    files:
      - sql/create-db.sql
